<%= form_with(model: publication, class: "my-form") do |form| %>
  <% if publication.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(publication.errors.count, "error") %> prohibited this publication from being saved:</h2>

      <ul>
        <% publication.errors.full_messages.each do |error| %>
          <li><%= error %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :Título, class: "form-label" %>
    <%= form.text_field :tittle, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :Imagen, class: "form-label" %>
    <%= form.text_field :image, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :Contenido, class: "form-label" %>
    <%= form.text_area :content, class: "form-control" %>
  </div>
  
  <div class="form-group">
    <%= form.submit "Guardar", class: "btn btn-dark mt-3"  %>
  </div>
<% end %>
